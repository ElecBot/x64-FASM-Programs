Contains projects that are bootable. These are mostly UEFI based programs but some may run after exiting UEFI "boot-services" mode.
The relevant shared assembly libraries and other files are included.
